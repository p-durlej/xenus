setpart - print and set XENUS partition sizes

setpart [-D device] [size1...]
setpart [-D device] -I

 -D device  specify device name (default is /dev/rhd0)
 -I         initialize the partition table with defaults

 size1...  partition sizes to set (default is to print partition sizes)
